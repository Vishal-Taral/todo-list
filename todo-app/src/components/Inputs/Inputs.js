import './Inputs.scss'

const Inputs = () => {
    return(
        <div>
            <input type="text" name="task" placeholder='Type here e.g. buy cake' />
        </div>
    );
}

export default Inputs 